# Core dependencies (Python 3.7 compatible)
voila>=0.2.0,<0.6.0
jupyter>=1.0.0
ipywidgets>=7.5.0,<9.0.0
notebook>=6.0.0,<7.0.0
ipyevents>=2.0.0

# No additional dependencies needed - dataclasses is built-in for Python 3.7+
